<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <packaging>rpm</packaging>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>cfe_39</artifactId>
    <version>${revision}${sha1}${changelist}.${env.TARGET_OS}.x86_64</version>
    <name>cfe_39</name>
    <description>cfe_39</description>
    <groupId>com.teragrep</groupId>
    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.source>1.8</maven.compiler.source>
        <maven.compiler.target>1.8</maven.compiler.target>
        <java.version>1.8</java.version>
        <revision>0.0.1</revision>
        <changelist>-SNAPSHOT</changelist>
        <sha1/>
    </properties>
    <build>
        <directory>${project.basedir}/target</directory>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <skipTests>true</skipTests>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>rpm-maven-plugin</artifactId>
                <version>2.2.0</version>
                <extensions>true</extensions>
                <executions>
                    <execution>
                        <id>default-rpm</id>
                        <goals>
                            <goal>rpm</goal>
                        </goals>
                        <phase>package</phase>
                    </execution>
                </executions>
                <configuration>
                    <summary>${project.groupId}-${project.artifactId}</summary>
                    <name>${project.groupId}-${project.artifactId}</name>
                    <version>${project.version}</version>
                    <release>${env.BUILD_ID}</release>
                    <license>Proprietary</license>
                    <distribution>Fail-Safe Log Management Suite</distribution>
                    <vendor>https://fail-safe.net/</vendor>
                    <packager>Fail-Safe &lt;servicedesk@fail-safe.net&gt;</packager>
                    <group>Fail-Safe/LogManagementSuite</group>
                    <description>${project.groupId}-${project.artifactId}</description>
                    <needarch>true</needarch>
                    <defaultUsername>srv-cfe_39</defaultUsername>
                    <defaultGroupname>srv-cfe_39</defaultGroupname>
                    <defaultFilemode>0600</defaultFilemode>
                    <defaultDirmode>0700</defaultDirmode>
                    <mappings>
                        <mapping>
                            <directory>/opt/teragrep/${project.artifactId}/lib</directory>
                            <directoryIncluded>true</directoryIncluded>
                            <sources>
                                <source>
                                    <location>${project.basedir}/target</location>
                                    <includes>
                                        <include>cfe_39.jar</include>
                                    </includes>
                                </source>
                            </sources>
                        </mapping>
                        <mapping>
                            <directory>/opt/teragrep/${project.artifactId}/etc</directory>
                            <directoryIncluded>true</directoryIncluded>
                            <configuration>noreplace</configuration>
                            <sources>
                                <source>
                                    <location>${project.basedir}/rpm/resources/application.properties</location>
                                </source>
                                <source>
                                    <location>${project.basedir}/rpm/resources/log4j2.xml</location>
                                </source>
                            </sources>
                        </mapping>
                        <mapping>
                            <directory>/usr/lib/systemd/system</directory>
                            <directoryIncluded>false</directoryIncluded>
                            <sources>
                                <source>
                                    <location>${project.basedir}/rpm/resources/cfe_39.service</location>
                                </source>
                            </sources>
                        </mapping>
                    </mappings>
                    <requires>
                        <require>java-1.8.0-openjdk-headless &gt;= 1.9.0</require>
                    </requires>
                    <preinstallScriptlet>
                        <script>
                            getent group srv-cfe_39 > /dev/null || groupadd -r srv-cfe_39;
                            getent passwd srv-cfe_39 > /dev/null || useradd -r -g srv-cfe_39 -d /opt/teragrep/${project.artifactId} -s /sbin/nologin -c "Service User" srv-cfe_39;
                            exit 0;
                        </script>
                    </preinstallScriptlet>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>2.8.2</version>
                <configuration>
                    <skip>true</skip>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.sonatype.plugins</groupId>
                <artifactId>nexus-staging-maven-plugin</artifactId>
                <version>1.6.8</version>
                <executions>
                    <execution>
                        <id>nexus-deploy</id>
                        <phase>deploy</phase>
                        <goals>
                            <goal>deploy</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <serverId>scrapyard.xnet.fi-teragrep-releases</serverId>
                    <nexusUrl>https://scrapyard.xnet.fi</nexusUrl>
                    <skipStaging>true</skipStaging>
                    <stagingRepositoryId>teragrep-releases</stagingRepositoryId>
                </configuration>
            </plugin>
        </plugins>
    </build>
    <distributionManagement>
        <repository>
            <id>scrapyard.xnet.fi-teragrep-releases</id>
            <url>https://scrapyard.xnet.fi/repository/teragrep-releases/</url>
        </repository>
    </distributionManagement>
</project>